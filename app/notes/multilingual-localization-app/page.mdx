import { EntryBanner } from "@/app/notes/components/entry-banner";
import { CopyToClipboard } from "@/app/notes/components/copy-to-clipboard";
import { multiLinguqalLocalizationApp } from "@/app/notes/state/entries";

<EntryBanner entryItem={multiLinguqalLocalizationApp} />

# Part A: Frontend

## Step 1: Create NextJS Project

We will run this command to start a new next js project

```


npx create-next-app@latest


```

<CopyToClipboard text="npx create-next-app@latest" />

```



```

---

```


```

## Step 2: Add chadcn

Next, lets add chadcn into our project

```


npx shadcn@latest init


```

<CopyToClipboard text="npx shadcn@latest init" />

```



```

---

```


```

## Step 3: Add i18next boilerplate code

\
Next lets add the i18n-next and friends into our app. We can use following command to do so:

```


npx myelino add i18next



```

<CopyToClipboard text="npx myelino add i18next" />

Running this will do the following:

```



```

**1. Install dependencies**

**2. Add i18-next boilerplate code**

```


```

Lets go over these two points in detail

## 1. Install the following dependencies:

**i18next**: It provides the core functionality for managing translations and handling different languages in your application.

\
**i18next-browser-languagedetector**: Automatically detects the user's language preference by checking browser settings, cookies, and query parameters.

\
**i18next-chained-backend**: Enables chaining of multiple backends and caches for loading translation resources, prioritizing local cache before remote fetching.

\
**i18next-resources-to-backend**: Transforms resources for lazy loading in i18next, allowing dynamic loading of translations to reduce initial application load time.

## 2. Add boilerplate code

You will notice that it also added the following boilerplate code under `libs/i18n-next` directory. Lets go over them one by one

```


```

- **`cookie-utils`**: contains getter and setter helper functions for dealing with cookies
- **`get-lang-and-direction`**: helper method for getting language and text direction
- **`i18n-config`**: contains i18nConfig and helper object
- **`i18n-next-html-provider`**: Helper component that wraps `html` with `I18NextProvider` and injects `lang` and `direction`
- **`i18n-next-provider`**: Which defines a custom `I18NextProvider` provider
- **`init`**: Contains `i18n` instance
- **`language-switcher`**: Helper component that helps you switch differnt languages
- **`use-translation`**: Custom hook that wraps the original `useTranslation` hook. You will see in a minute why :)

```



```

---

## Step 4: Wrap `layout.tsx`

```jsx

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <I18NextHtmlProvider>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </I18NextHtmlProvider>
  );
}


```

```



```

## Step 5: Create locales

\
`locales/en/common.json`

```
{
    "title": "Welcome to the app"
}


```

<CopyToClipboard
  text={JSON.stringify(
    {
      title: "Welcome to the app",
    },
    null,
    4
  )}
/>

---

```



```

## Step 6: Use the translations

```

"use client";

import { Button } from "@/components/ui/button";
import { useTranslation } from "@/libs/i18n-next/use-translation";

export const Banner = () => {
  const { t } = useTranslation(["banner", "common"]);
  return (
    <section className="flex flex-col justify-center items-center mt-32">
      <h1 className="text-5xl font-bold">{t("title")}</h1>

      <p className="mt-4 text-xl font-mono">{t("description")}</p>

      <Button className="rounded-full mt-4">{t("cta")}</Button>
    </section>
  );
};



```

```



```

---

```



```

# Part 2: Multi Lingual Translation with Myelin AI

```


```

Alright, now we have the frontend, we are ready to start our translation infra pipeline with [myelin](https://www.myelin.dev)

## Step 2: Initialize myelin

```

> npx myelino



```

This will generate `myelin.config.json`

```

{
    "aiProvider": "deepseek",
    "aiModel": "deepseek-chat",
    "locale": {
        "location": "locales",
        "sourceLanguage": "en",
        "targetLanguages": [
            "es",
            "fr",
            "zh",
            "zh-TW",
            "en-US"
        ]
    }
}

```

<CopyToClipboard
  text={JSON.stringify(
    {
      aiProvider: "deepseek",
      aiModel: "deepseek-chat",
      locale: {
        location: "locales",
        sourceLanguage: "en",
        targetLanguages: ["es", "fr", "zh", "zh-TW", "en-US"],
      },
    },
    null,
    4
  )}
/>

## Step 2: Run Translate

```

> npx myelino translate


```

<CopyToClipboard text={"npx myelino translate"} />

```



```

When you run this command, Myelin loads your source JSON file (e.g., locales/en.json), identifies any new or updated translation strings, produces translations for your specified target languages, and creates or updates the relevant target language JSON files (e.g., locales/ko.json, locales/zh.json, locales/fr.json).

```


```

## Step 3: Import `language-switcher` component to test it out

```

import { LanguageSwitcher } from "@/libs/i18n-next/language-switcher";
import { Banner } from "./components/banner";

export default function Home() {
  return (
    <div>
      <LanguageSwitcher />
      <Banner />
    </div>
  );
}


```

```



```

And thats it, you have built your own multi lingual app, chadcn style. Well done!

```



```

---

```



```

# Bonus: Automatic intellisense with TypeScript

```


```

If you noticed, when using `use-translate`, its not very optimal. We can improve the DX by using the power of typescript types.

```


```

Typically we do that by creating `types/i18next.d.ts` file and import our translations like. However, this is a manual process and can be time consuming. Luckily there is a cli from myelin :).

```


npx myelino upsert i18next.d.ts



```

<CopyToClipboard text={"npx myelino upsert i18next.d.ts"} />

This script will automatically generate types for you. Now if you try to fill in the keys, the options should be automatically populated for you. Thats it. If you made it this far, then great work!
